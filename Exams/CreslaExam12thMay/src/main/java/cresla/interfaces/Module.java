package cresla.interfaces;

public interface Module extends Identifiable { //TODO just extends it, nothing else. Can't touch the interfaces tho!!!
}
//i guess it will only serve for being a type of data which a collection can have
